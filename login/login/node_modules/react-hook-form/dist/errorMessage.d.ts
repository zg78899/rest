import * as React from 'react';
import { FieldName, FormValuesFromErrors } from './types';
declare type Props<Errors, Name> = {
    as?: React.ElementType<any> | React.FunctionComponent<any> | string | any;
    errors?: Errors;
    name: Name;
};
declare const ErrorMessage: <Errors extends import("./types").NestDataObject<any>, Name extends FieldName<FormValuesFromErrors<Errors>>>({ as: InnerComponent, errors: errorsFromProps, name, }: Props<Errors, Name>) => JSX.Element | null;
export { ErrorMessage };
