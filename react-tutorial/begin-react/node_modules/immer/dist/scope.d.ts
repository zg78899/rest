import { Patch, PatchListener } from "./types";
/** Each scope represents a `produce` call. */
export declare class ImmerScope {
    static current?: ImmerScope;
    patches?: Patch[];
    inversePatches?: Patch[];
    canAutoFreeze: boolean;
    drafts: any[];
    parent?: ImmerScope;
    patchListener?: PatchListener;
    constructor(parent?: ImmerScope);
    usePatches(patchListener: PatchListener): void;
    revoke(): void;
    leave(): void;
    static enter(): ImmerScope;
}
